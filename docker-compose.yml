version: '2'

services:
  iapweb:
    build: .
    image: iapweb
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    restart: unless-stopped
